info:
  name: ChangeEmail
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/account/email"
  body:
    type: json
    data: |-
      {
        "email": "vanvooren.lucas@gmail.com",
        "currentPassword": "Password1!"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should return 204", function () {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
