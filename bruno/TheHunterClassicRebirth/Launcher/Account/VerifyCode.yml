info:
  name: VerifyCode
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/account/email-verification/verify"
  body:
    type: json
    data: |-
      {
        "code": "137753"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should return 204", function () {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
